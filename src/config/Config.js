export const categoryOptions = Object.freeze(["All", "Education", "E-commerce", "Health"]);

export const defaultOptions = Object.freeze(["Default", "Ascending", "Descending"]);

export const advertMsg =
  "Tada! Get started with a free template. Can’t find what you are looking for? Search from the 1000+ available templates";
